// Чтобы не делать импорт в каждом элементе
// создаем однифайл для импорта и подключаем ко всем страницам

import SerhiiBtn from "@/components/UI/Serhiibtn";
import SerhiiInput from "@/components/UI/SerhiiDialog";
import SerhiiDialog from "@/components/UI/SerhiiInput";
import SerhiiSelect from "@/components/UI/SerhiiSelect";

export default {
    SerhiiBtn, SerhiiInput, SerhiiDialog, SerhiiSelect
}